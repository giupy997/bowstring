[build]
  base = "web"
  command = "npm run build"
  # Note: the Netlify CLI resolves `publish` from the repo root (not from
  # `base`), so this must be the root-relative path.
  publish = "web/.next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
